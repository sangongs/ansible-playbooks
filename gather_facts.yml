---

- hosts: all
  gather_facts: true 
  tasks:
    - name: set a fact man
      set_fact:
        foo: "bar"
        a:
          b:
            c:
              - "a"
              - "b"
        cacheable: {{ cutom_fact_cacheable | default(false) }}
